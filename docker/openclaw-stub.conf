server {
    listen 18789 default_server;
    server_name _;
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
        add_header X-Stub "openclaw-gateway-stub";
    }
}
